# Archived Toy Public Root

This directory preserves the older toy public-root files that used to sit in the main path.

They were moved here after the recovered `null-mint` / `paradox` track was promoted into the canonical root.

Use this directory only if you are auditing how the public repo evolved. Do not treat it as the current integration target.
